class Solution {
    public int rob(int[] nums) {
        
    }
    int fun(int[] arr){
        if(dp[n] != -1) return dp[n];
        // take step 
        int take = arr[i] + fun(i+1,nums);
        //skip step
        int skip = 
    }
}